def run_gpt_baseline(question: str, model: str = "gpt-4o-mini") -> dict:
    # Placeholder deterministic baseline; replace with real API if desired.
    return {"answer": f"(Baseline {model}) {question}", "confidence": 1.0}
